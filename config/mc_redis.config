[
    % mc_redis
    {mc_redis, [
        {host, "{{redis_host}}"},         % redis host
        {port, {{redis_port}}},           % redis port
        {query_timeout, 5000},  % In milliseconds
        {poolgirl, [
            {size, 10}          % Number of pool workers
        ]}
    ]}
].

